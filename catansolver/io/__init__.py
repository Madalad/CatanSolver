"""IO layer: the canonical pydantic state schema.

This schema is the project's contract — the manual-input format, the HTTP API
payload between the web UI and the solver, and the target the Catanatron adapter
maps to/from.
"""
